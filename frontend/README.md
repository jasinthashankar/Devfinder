# DevFinder Frontend

React + Vite + Tailwind CSS v4 frontend. See the root `README.md`, `QUICKSTART.md`, and `INSTALLATION.md` for full setup instructions.

```bash
npm install
cp .env.example .env
npm run dev
```
